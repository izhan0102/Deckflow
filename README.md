# Deckflow
DeckFlow is an AI presentation builder. You write a short prompt, pick a theme, and get an editable deck in seconds. Drag the text boxes around, edit them inline, swap colors, ask the chat to rewrite a slide, drop in your own images, and export to PowerPoint or PDF. Built with Next.js and Groq.
